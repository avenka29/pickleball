VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional. Only needed if you deploy/use the add-user Edge Function for CLI-style admin actions.
# The core app can run without this: admins can manage whitelist/theme/Elo through RLS-backed UI,
# and whitelisted Google users bootstrap their profile automatically on first sign-in.
SUPABASE_SERVICE_ROLE_KEY=
